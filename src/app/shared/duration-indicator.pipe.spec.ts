import { DurationIndicatorPipe } from './duration-indicator.pipe';

describe('DurationIndicatorPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationIndicatorPipe();
    expect(pipe).toBeTruthy();
  });
});
